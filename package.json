{
  "name": "react-ui",
  "version": "1.0.0",
  "engines": {
    "node": "6.10.x"
  },
  "scripts": {
    "start": "node server",
    "deploy": "git push heroku master",
    "dev": "nodemon server",
    "heroku-postbuild": "cd react-ui/ && npm install --only=dev && npm install && npm run build"
  },
  "cacheDirectories": [
    "node_modules",
    "react-ui/node_modules"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/simonlimon/varsncrafts.git"
  },
  "license": "MIT",
  "devDependencies": {
    "nodemon": "^1.11.0"
  },
  "dependencies": {
    "axios": "^0.15.3",
    "express": "^4.15.2",
    "google-images": "^2.1.0",
    "request": "^2.81.0",
    "sharp": "^0.17.2",
    "uid": "0.0.2"
  }
}
